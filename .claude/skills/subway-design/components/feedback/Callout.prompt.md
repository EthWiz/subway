Where the product states a mechanic or a risk in the flow it applies to — never as fine print.

```jsx
<Callout tone="info" title="You mint at the floor">
  Unsettled hedge profit counts as zero until the daily epoch, so the price you mint at is slightly
  below fair. That asymmetry is what makes deposit-time manipulation pointless.
</Callout>
```

`danger` is reserved for exit/panic paths and jurisdiction blocks.
