Shows the vault's concentrated range against the feed price. The marker is always the feed, never the pool tick.

```jsx
<RangeMeter lower={112} upper={131} price={121.4}
  lowerLabel="$112.20 −6%" upperLabel="$131.00 +6%" priceLabel="$121.40 feed" inRange />
```
