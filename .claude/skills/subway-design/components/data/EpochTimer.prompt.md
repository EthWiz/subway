Hedged-vault epoch clock. Only appears on `h…` surfaces — the unhedged vault has no epoch.

```jsx
<EpochTimer epoch={412} countdown="04:12:08" progress={68}
  note="Settles daily at 21:00 ET. Redemptions claim after settlement." />
```
