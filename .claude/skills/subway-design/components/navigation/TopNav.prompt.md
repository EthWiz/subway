App header for every product surface. The wordmark is set in Instrument Serif at 26px — Subway has no logo mark.

```jsx
<TopNav active="vaults" onNavigate={go} address="0x4f2a…9c1b" />
```

Height is fixed at 64px; the chain chip always names the chain the app is pointed at.
