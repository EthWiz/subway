Icon-only control for row actions, table headers and dialog dismiss; always pass `label`.

```jsx
<IconButton name="x" label="Close" />
<IconButton name="refresh-cw" label="Refresh NAV" variant="outline" />
```
