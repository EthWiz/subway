Pill used for filters on the vault list ("Hedgeable", "v4 pools", "Cleared screen").

```jsx
<Tag selected onClick={() => …}>Hedgeable</Tag>
<Tag onRemove={() => …}>INTC</Tag>
```

Sentence case, 26px tall, fully rounded — the only fully-rounded shape in the system besides StatusPill.
