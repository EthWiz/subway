The system's number: mono tabular figure under a mono uppercase label.

```jsx
<Stat label="TVL" value="$1.24M" delta="+3.1%" hint="across 3 pairs" />
<Stat label="Trailing 24h fee APR" value="10.3" unit="%" size="lg" />
```

Always pair a yield figure with a `hint` that states its window — the brand never shows an APR without saying it decays.
