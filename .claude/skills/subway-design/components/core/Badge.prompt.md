Mono uppercase status label — phase gates, vault state, jurisdiction flags.

```jsx
<Badge tone="positive" icon="circle-check">In range</Badge>
<Badge tone="warning">Phase 3 — not built</Badge>
```

Tones map to the semantic colour set; `accent` is reserved for brand-level labels (e.g. "Floor NAV"), not for status.
