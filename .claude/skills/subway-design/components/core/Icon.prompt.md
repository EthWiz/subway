Lucide glyph that inherits `currentColor` — the only icon primitive in the system; never inline hand-drawn SVG.

```jsx
<Icon name="arrow-up-right" size={14} />
```

Names are Lucide kebab-case. Sizes used in product: 14 inside buttons and pills, 16 in rows and nav, 20 in callouts and empty states.
